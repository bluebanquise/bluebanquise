---
- name: "hostname █ Set {{ inventory_hostname }} hostname"
  hostname:
    name: "{{ set_hostname_fully_qualified_domaine_name | ternary( inventory_hostname + '.' + domain_name, inventory_hostname ) }}"
  tags:
    - identify
