---
- name: "template █ Generate /etc/hosts {% if direct_access is defined %}with direct_access enabled{% endif %}"
  template:
    src: hosts.j2
    dest: /etc/hosts
    owner: root
    group: root
    mode: 0644
  tags:
    - template
