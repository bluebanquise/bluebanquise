---

- name: "service █ Disable wicked"
  service:
    name: wicked
    enabled: no
  notify: reboot █ Reboot system

- name: "service █ Enable NetworkManager"
  service:
    name: NetworkManager
    enabled: yes
