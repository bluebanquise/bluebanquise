---
- name: include_vars ░ Gather OS specific variables
  # This task gathers variables defined in OS specific files.
  #
  # Search vars in:
  #  - <distribution>_<major>.yml    # eg. CentOS_8.yml
  #  - <os_family>_<major>.yml       # eg. RedHat_8.yml
  #  - <distribution>.yml            # eg. CentOS.yml
  #  - <os_family>.yml               # eg. RedHat.yml
  #
  # If no OS specific file is found, the role will default to vars/main.yml
  #
  include_vars: "{{ item }}"
  with_first_found:
    - files:
        - "vars/{{ ansible_facts.distribution | replace(' ','_') }}_{{ ansible_facts.distribution_major_version }}.yml"
        - "vars/{{ ansible_facts.os_family }}_{{ ansible_facts.distribution_major_version }}.yml"
        - "vars/{{ ansible_facts.distribution | replace(' ','_') }}.yml"
        - "vars/{{ ansible_facts.os_family | upper }}.yml"
        - "vars/{{ ansible_facts.os_family }}.yml"
      skip: true
  tags:
    - always

- name: firewalld █ "Add services to firewall's {{ dns_server_firewall_zone | default('public') }} zone"
  firewalld:
    zone: "{{ dns_server_firewall_zone | default('public') }}"
    service: "{{ item }}"
    immediate: "yes"
    permanent: "yes"
    state: enabled
  when:
    - ansible_facts.os_family == "RedHat" or ansible_facts.os_family == "Suse"
    - ep_firewall | default(false) | bool
  loop: "{{ dns_server_firewall_services_to_add }}"
  tags:
    - firewall

- name: "package █ Install {{ dns_server_packages_to_install | join(' ') }} packages"
  package:
    name: "{{ dns_server_packages_to_install }}"
    state: present
  tags:
    - package

- name: file █ Run named outside of chroot on SuSE base systems
  lineinfile:
    path: /etc/sysconfig/named
    regexp: '^NAMED_RUN_CHROOTED='
    line: 'NAMED_RUN_CHROOTED="no"'
  when: ansible_facts.os_family == "Suse"
  notify: service █ Restart dns services

- name: file █ Create needed directory /var/named
  file:
    path: /var/named
    state: directory
    owner: root
    group: named
    mode: 0775

- name: file █ Create needed directories under /var/named
  file:
    path: /var/named/{{ item }}
    state: directory
    owner: root
    group: named
    mode: 0770
  with_items:
    - data
    - dynamic

- name: template █ Generate /etc/named.conf
  template:
    src: named.conf.j2
    dest: /etc/named.conf
    owner: root
    group: root
    mode: 0644
  notify: service █ Restart dns services
  tags:
    - template

- name: template █ Generate /etc/named.rfc1912.zones for SuSE based systems
  template:
    src: named.rfc1912.zones.j2
    dest: /etc/named.rfc1912.zones
    owner: root
    group: root
    mode: 0644
  notify: service █ Restart dns services
  when: ansible_facts.os_family == "Suse"
  tags:
    - template

- name: file █ Created /var/named/named.ca for SuSE based systems
  copy:
    src: named.ca
    dest: /var/named/named.ca
    owner: root
    group: root
    mode: 0644
  notify: service █ Restart dns services
  when: ansible_facts.os_family == "Suse"

- name: template █ Generate /etc/named.root.key for SuSE based systems
  template:
    src: named.root.key.j2
    dest: /etc/named.root.key
    owner: root
    group: root
    mode: 0644
  notify: service █ Restart dns services
  when: ansible_facts.os_family == "Suse"
  tags:
    - template

- name: template █ Generate Forward zone file on Master DNS /var/named/forward
  template:
    src: forward.j2
    dest: /var/named/forward
    owner: root
    group: root
    mode: 0644
  register: zoneconf
  tags:
    - template

- name: set_fact ░ Set SOA Serial for reverse zone
  set_fact:
    serial: "{{ lookup('pipe', 'date +%s') }}"
  when: zoneconf.changed  # noqa 503
  tags:
    - template
    - internal

- name: template █ Generate SOA for forward zone file /var/named/forward.soa
  template:
    src: forward.soa.j2
    dest: /var/named/forward.soa
    owner: root
    group: root
    mode: 0644
  notify: service █ Restart dns services
  when: zoneconf.changed  # noqa 503
  tags:
    - template

- name: template █ Generate Reverse zone file on Master DNS /var/named/reverse
  template:
    src: reverse.j2
    dest: /var/named/reverse
    owner: root
    group: root
    mode: 0644
  tags:
    - template

- name: template █ Generate SOA for reverse zone file /var/named/reverse.soa
  template:
    src: reverse.soa.j2
    dest: /var/named/reverse.soa
    owner: root
    group: root
    mode: 0644
  notify: service █ Restart dns services
  when: zoneconf.changed  # noqa 503
  tags:
    - template

- name: meta █ Run handler tasks to restart dns services
  meta: flush_handlers

- name: "service █ Manage {{ dns_server_services_to_start | join(' ') }} state"
  service:
    name: "{{ item }}"
    enabled: "{{ (enable_services | bool) | ternary('yes','no') }}"
    state: "{{ (start_services | bool) | ternary('started', omit) }}"
  loop: "{{ dns_server_services_to_start }}"
  tags:
    - service
