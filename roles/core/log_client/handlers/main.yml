---

- name: Restart rsyslog services
  service:
    name: "{{ item }}"
    state: restarted
  loop: "{{ log_client_services_to_start }}"
  when:
    - "'service' not in ansible_skip_tags"
    - (start_services | bool)
