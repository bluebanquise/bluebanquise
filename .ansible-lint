warn_list:
  - role-name  # Role name {0} does not match ``^[a-z][a-z0-9_]+$`` pattern
  - no-changed-when
verbosity: 1
skip_list:
  - fqcn-builtins
exclude_paths:
  - ./roles/core/pxe_stack/tasks/client_os/DGX.yml
  - ./github/