### General settings file

# Localisation
time_zone: Europe/Paris

# Internal mechanisms, use with care
icebergs_system: false # Activate or deactivate icebergs topology system
accelerated_mode: false

# /etc/hosts
hosts_file:
  range: all # can be all (all hosts) or iceberg (iceberg only if you want to isolate icebergs)
