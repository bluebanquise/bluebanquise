---

pcs_packages_to_install:
  - pacemaker
  - corosync
  - pcs
  - fence-agents
  - resource-agents

pcs_resource_op_defaults_update: 'pcs resource op defaults update'
pcs_resource_defaults_update: 'pcs resource defaults update'
