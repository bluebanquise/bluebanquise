---
- name: "package <|> Install custom packages for {{ item }}"
  ansible.builtin.package:
    name: "{{ custom_packages[item] }}"
    state: present
  loop: "{{ custom_packages.keys() | list }}"
  when: item in group_names
